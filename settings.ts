
/**
 * Will save all the options selected in the options menu.
 * I'd like to read from a config file, just because
 * I haven't played around with that yet and I feel like
 * it'd be fun, but if there's a correct way we should
 * do that instead, just to keep it more 'professional'. 
 */
export class Settings{

    getSettings(){

    }

    setSettings(){

    }
}